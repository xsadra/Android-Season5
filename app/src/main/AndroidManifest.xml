<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.sadra.apps.season5">

    <application
        android:name=".app.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".episodes.E01AndroidCustomSpinner" />
        <activity android:name=".episodes.E02AndroidCustomListView" />
        <activity android:name=".episodes.E03AndroidCustomGridView" />
        <activity
            android:name=".episodes.E04AndroidRecyclerView"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".episodes.E04AndroidRecyclerViewItemActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".episodes.E05AndroidTabWidget" />
        <activity
            android:name=".episodes.E06AndroidBasicActivity"
            android:label="@string/title_activity_e06_android_basic"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".episodes.E07AndroidActionbarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>